apiVersion: v1
kind: Service
metadata:
  name: integration-service-blue
spec:
  type: LoadBalancer
  selector:
    app: integration-service
    version: blue
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001