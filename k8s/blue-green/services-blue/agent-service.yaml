apiVersion: v1
kind: Service
metadata:
  name: agent-service-blue
spec:
  type: LoadBalancer
  selector:
    app: agent-service
    version: blue
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
